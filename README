fmenu - fast menu
=================
fmenu is a generic and efficient menu for X. 


Requirements
------------
In order to build fmenu you need the Xlib header files.


Installation
------------
Edit config.mk to match your local setup (fmenu is installed into
the /usr/local namespace by default).

Afterwards enter the following command to build and install fmenu (if
necessary as root):

    make clean install


Running fmenu
-------------
See the man page for details.
